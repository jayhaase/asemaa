<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ title }}</title>
        <link rel="stylesheet" href="{{ '/css/styles.css' | url }}">
        <link rel="stylesheet" href="{{ '/css/tailwind.css' | url }}">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link
            href="https://fonts.googleapis.com/css2?family=Noto+Sans:wght@400;700&family=Nunito:wght@400;700&family=Cinzel:wght@400;700&display=swap"
            rel="stylesheet">
    </head>
    <body>
        <div
            class="flex items-center justify-center py-4 bg-gray-100">
            <!-- Container for three cards in a row -->
            <div
                class="flex space-x-6 max-w-5xl w-full">
                <!-- Left Card (33%) -->
                {% set cardText = { title:leftPanelTitle, subtitle: leftPanelSubtitle } %}
                {% include "_heading_side_card.njk" %}

                <!-- Center Card (33%) -->
                <div class="bg-white border border-gray-300 rounded-lg shadow-lg p-6 w-1/3 max-w-md text-center flex flex-col justify-center
                        h-full h-50">
                    <h1 class="font-heading text-5xl font-semibold text-gray-800">{{ title }}</h1>
                    <h2 class="font-heading text-xl mt-2 text-gray-600">{{ subtitle }}</h2>
                </div>
                <!-- Right Card (33%) -->
                {% set cardText = { title:rightPanelTitle, subtitle: rightPanelSubtitle } %}
                {% include "_heading_side_card.njk" %}
            </div>
        </div>
        {# <h2 class="ml-5 font-header  text-1xl font-bold py-6">{{ pageTitle }}</h2> #}
        <main class="content font-body max-w-3xl mx-auto py-6 min-h-screen">
            {{ content | safe }}
            <div class="flex justify-center items-center">
                <img src="/images/turtle-island.png" alt="Turtle Island" class="map">
            </div>
            <div class="pointers">
                {% for location in locations %}
                    <div class="pointer" style="top: {{ location.y }}%; left: {{ location.x }}%;" data-location="{{ location.id }}" data-title="{{ location.name }}" data-description="{{ location.description }}">
                        this is a thing!
                    </div>
                {% endfor %}
            </div>
        </main>
        <div class="modal micromodal-slide" id="modal-1" aria-hidden="true">
            <div tabindex="-1" class="modal__overlay" data-micromodal-close>
                <div role="dialog" class="modal__container">
                    <header class="modal__header">
                        <h2 class="modal__title">Modal Title</h2>
                        <button class="modal__close" data-micromodal-close aria-label="Close modal"></button>
                    </header>
                    <main class="modal__content" id="modal-1-content">Modal content here</main>
                </div>
            </div>
        </div>
        <script src="https://unpkg.com/micromodal@0.4.10/dist/micromodal.min.js"></script>
        <script src="/js/scripts.js"></script>
    </body>
</html>